name: Git Conventions

on:
    pull_request:
        branches:
            - develop

jobs:
    ensure-linear-history:
        name: Ensure Linear Git History
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v6
              with:
                  fetch-depth: 0
                  ref: ${{ github.head_ref }}

            - name: Run Check
              uses: SE-UUlm/snowballr-ci/src/ensure-linear-history@feat/1-feature-add-initial-workflows
              with:
                  target-branch: develop

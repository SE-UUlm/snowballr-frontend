version: 2
updates:
    - package-ecosystem: "github-actions"
      directory: "/"
      schedule:
          interval: "monthly"

    - package-ecosystem: "npm"
      directory: "/"
      schedule:
          interval: "monthly"
      versioning-strategy: "increase"
      groups:
          vitest:
              patterns:
                  - "@vitest/*"
                  - "vitest"
          protobuf-ts:
              patterns:
                  - "@protobuf-ts/*"
